using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEngine.UI;

public class PanelAdministradorManager : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] private GameObject panelAdministracionDinero;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
